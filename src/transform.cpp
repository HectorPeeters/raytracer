#include "transform.h"

// #include <glm/gtc/matrix_transform.hpp>
// #include <glm/gtx/euler_angles.hpp>

void transform_update_matrices(struct transform_t *transform) {
  // glm::mat4 translated = glm::translate(glm::mat4(), transform->position);
  // glm::mat4 rotated = glm::eulerAngleYXZ(
      // transform->rotation.y, transform->rotation.x, transform->rotation.z);
  // glm::mat4 scaled = glm::scale(glm::mat4(), transform->scale);

  // transform->matrix = translated * rotated * scaled;
  // transform->inv_matrix = glm::inverse(transform->matrix);
  UNREACHABLE();
}
